name: Computer Prachi Automatic Updates

on:
  schedule:
    # GitHub uses UTC. Runs every 30 minutes.
    - cron: "*/30 * * * *"
  workflow_dispatch:
  push:
    branches:
      - main
      - master

permissions:
  contents: write

concurrency:
  group: computer-prachi-auto-update
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout website
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install updater dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Fetch and update Jobs / Results / Admit Card
        run: python auto_update.py

      - name: Commit changed pages
        run: |
          git config user.name "Computer Prachi Auto Update"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html all-jobs.html all-results.html all-admit-card.html all-answer-key.html all-admission.html all-iti-jobs.html all-outsourcing-jobs.html all-syllabus.html all-documents-verification.html all-latest-update.html auto_update.py
          if git diff --cached --quiet; then
            echo "No new updates found."
          else
            git commit -m "Auto update jobs results admit cards"
            git push
          fi
